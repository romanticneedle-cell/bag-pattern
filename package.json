{
  "name": "bag-pattern-generator",
  "private": true,
  "version": "0.3.0",
  "type": "module",
  "description": "낭만바늘 가방 패턴 생성기 — 완성 치수 → 1:1 실치수 재단 도안 (SVG 미리보기 + A4 분할 PDF)",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@pdf-lib/fontkit": "^1.1.1",
    "pdf-lib": "^1.17.1"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^5.4.10",
    "vitest": "^2.1.4"
  }
}
